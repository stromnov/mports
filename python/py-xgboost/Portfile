# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4
# $Id$

PortSystem          1.0
PortGroup           github 1.0
PortGroup           python 1.0

github.setup        dmlc xgboost c529cac6ff5e5d9a3affb3d14af66d46537c5fd6

name                py-xgboost
version             20160813
revision            0
categories-append   math science
platforms           darwin
license             Apache
supported_archs     noarch

python.versions     27 35

maintainers         stromnov openmaintainer

description         XGBoost Python Package
long_description    ${description}

checksums           rmd160  f2c636489a8756b492bc31be503c28e051a5a51f \
                    sha256  a74bdc32e46aa4a6826a914585bfc06b4e0de77be1ec1c3104cd167de76c6453

if {${name} ne ${subport}} {
    worksrcdir          ${python.rootname}-${github.version}/python-package

    patchfiles-append   patch-xgboost_libpath.py.diff \
                        patch-setup.py.diff \
                        patch-MANIFEST.in.diff

    post-patch {
        reinplace "s|@prefix@|${prefix}|g" ${worksrcpath}/xgboost/libpath.py
    }

    depends_build-append \
                        port:py${python.version}-setuptools
    depends_lib-append  port:xgboost \
                        port:py${python.version}-numpy \
                        port:py${python.version}-scipy

    livecheck.type      none
}
