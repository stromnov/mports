# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           python 1.0

name                py-smac
version             0.11.1
revision            0
categories-append   devel
platforms           darwin
license             BSD
supported_archs     noarch

python.versions     37

maintainers         {stromnov @stromnov} openmaintainer

description         SMAC3, a Python implementation of 'Sequential Model-based Algorithm Configuration'.
long_description    ${description}

homepage            https://pypi.python.org/pypi/smac/
master_sites        pypi:[string index ${python.rootname} 0]/${python.rootname}

distname            ${python.rootname}-${version}

checksums           rmd160  efe566e12f6ca1da7336f2a7cce19fa3f3a70173 \
                    sha256  015f1f54dea309cd2e581f08e134ca5e5c604cd56ed6a9295edc384914199695 \
                    size    161313

if {${name} ne ${subport}} {
    depends_lib-append  port:py${python.version}-setuptools \
                        port:py${python.version}-cython \
                        port:py${python.version}-numpy \
                        port:py${python.version}-scipy \
                        port:py${python.version}-six \
                        port:py${python.version}-psutil \
                        port:py${python.version}-pynisher \
                        port:py${python.version}-configspace \
                        port:py${python.version}-scikit-learn \
                        port:py${python.version}-pyrfr \
                        port:py${python.version}-sphinx \
                        port:py${python.version}-sphinx_rtd_theme \
                        port:py${python.version}-joblib \
                        port:py${python.version}-nose \
                        port:py${python.version}-pyDOE \
                        port:py${python.version}-sobol_seq \
                        port:py${python.version}-statsmodels \
                        port:py${python.version}-emcee \
                        port:py${python.version}-george

    livecheck.type      none
}
